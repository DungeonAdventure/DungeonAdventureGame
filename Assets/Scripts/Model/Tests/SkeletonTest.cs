/*
using NUnit.Framework;
using Model;

public class SkeletonTests
{
    [Test]
    public void Skeleton_Constructor_InitializesCorrectly()
    {
        Skeleton skeleton = new Skeleton();

        Assert.AreEqual("Skeleton", skeleton.Name);
        Assert.AreEqual(100, skeleton.HitPoints);
        Assert.AreEqual(30, skeleton.MinDamage);
        Assert.AreEqual(50, skeleton.MaxDamage);
        Assert.AreEqual(3, skeleton.AttackSpeed);
        Assert.AreEqual(2, skeleton.MoveSpeed);
        Assert.AreEqual(0.8f, skeleton.ChanceToCrit);
        Assert.AreEqual(0.3f, skeleton.ChanceToHeal);
        Assert.AreEqual(30, skeleton.MinHeal);
        Assert.AreEqual(50, skeleton.MaxHeal);
    }

    [Test]
    public void Skeleton_CustomName_SetsNameCorrectly()
    {
        Skeleton skeleton = new Skeleton("Bone Lord");

        Assert.AreEqual("Bone Lord", skeleton.Name);
    }
}
*/